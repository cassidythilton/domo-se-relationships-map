// Deprecated in V1 of the rethink. Kept on disk for future reference.
// The original implementation depended on per-SC specialization data that
// is no longer part of the data model.
export function SpecialistMap() {
  return null;
}
