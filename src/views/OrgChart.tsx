// Replaced in V1 by the bipartite ribbon ("Full org" lens). Kept on disk
// for reference — the SC org tree on the left side of the ribbon is the
// modern equivalent.
export function OrgChart() {
  return null;
}
